'use strict';

let tails = 0;
let heads = 0;
let flip;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

flip = Math.random();
flip = Math.floor(flip * 2 + 1);
if (flip === 1)
    tails++;
else
    heads++;

document.write('heads: ' + heads);
document.write('tails: ' + tails);
